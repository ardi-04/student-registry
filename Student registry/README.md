# Student Registry CLI

A command-line application to manage student records.
Built with Python — stores and loads data from a text file.

## Features
- Add students with grades
- View all students
- Persistent storage